<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="12dp"
    android:layout_marginVertical="6dp"
    app:cardBackgroundColor="@color/white"
    app:cardCornerRadius="10dp"
    app:cardElevation="2dp"
    app:strokeColor="@color/border_light"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="14dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/tvProdutoNome"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:ellipsize="end"
                    android:maxLines="2"
                    android:textColor="@color/text_primary"
                    android:textSize="16sp"
                    android:textStyle="bold"
                    tools:text="Arroz 5kg" />

                <TextView
                    android:id="@+id/tvProdutoPrecoMax"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:textColor="@color/text_secondary"
                    android:textSize="12sp"
                    android:visibility="gone"
                    tools:text="Preço máximo: R$ 25,00"
                    tools:visibility="visible" />
            </LinearLayout>

            <ImageButton
                android:id="@+id/btnSetPreco"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Definir preço máximo"
                android:src="@drawable/ic_edit"
                app:tint="@color/wofertas_blue_primary" />

            <ImageButton
                android:id="@+id/btnDeleteProduto"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Remover produto"
                android:src="@drawable/ic_delete"
                app:tint="@color/status_suspenso" />
        </LinearLayout>

        <TextView
            android:id="@+id/tvStatusComparacao"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:background="@drawable/bg_cart_match"
            android:paddingHorizontal="10dp"
            android:paddingVertical="7dp"
            android:textColor="@color/status_ativo"
            android:textSize="12sp"
            android:textStyle="bold"
            tools:text="Melhor oferta encontrada" />

        <TextView
            android:id="@+id/tvProdutoEncontrado"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:textColor="@color/text_primary"
            android:textSize="14sp"
            android:textStyle="bold"
            android:visibility="gone"
            tools:text="Mercado Central - R$ 18,90"
            tools:visibility="visible" />

        <TextView
            android:id="@+id/tvOutrosMercados"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="3dp"
            android:textColor="@color/text_secondary"
            android:textSize="12sp"
            android:visibility="gone"
            tools:text="Também em Mercado Sul R$ 19,50; Mercado Norte R$ 21,90"
            tools:visibility="visible" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnVerOfertaCarrinho"
            style="@style/Widget.Material3.Button.OutlinedButton"
            android:layout_width="wrap_content"
            android:layout_height="42dp"
            android:layout_marginTop="10dp"
            android:minWidth="0dp"
            android:paddingHorizontal="14dp"
            android:text="Ver oferta"
            android:textAllCaps="false"
            android:textColor="@color/wofertas_blue_primary"
            android:visibility="gone"
            app:cornerRadius="10dp"
            app:icon="@drawable/ic_pdf"
            app:iconTint="@color/wofertas_blue_primary"
            app:strokeColor="@color/wofertas_blue_primary"
            tools:visibility="visible" />

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
